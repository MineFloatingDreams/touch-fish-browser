# 测试记录

测试日期：2026-07-16
目标平台：Windows x64

## 自动化测试范围

- URL 自动补全、非法协议、空值与超长输入。
- 设置清洗、透明度范围、窗口尺寸和可视区域约束。
- 标签栏和浏览操作栏合并为一行，支持持久化收起，并在 `84px` 与 `42px` 网页起始位置之间正确切换。
- 老板键格式、危险组合拒绝与隐藏/最小化恢复状态机。
- 老板键可选广告画面或直接静音隐藏；两种动作均验证网页静音及再次按键后的窗口位置、尺寸、可见性和原有音频状态恢复。
- 关闭按钮行为可配置为隐藏到托盘或完全退出，并同时作用于普通窗口与广告窗口。
- 鼠标移出透明功能会强制开启置顶，并在移回时恢复此前透明度；置顶和移出透明均不应用于广告模式。
- 打包前清理仅删除已识别的安装包、便携版、blockmap、builder 配置、解包目录及 electron-builder 遗留的 `.tmp` 解包目录，保留源码及其他文件。
- GitHub Actions 仅在 `v*.*.*` 标签推送时发布，并校验标签与 `package.json` 版本一致；云端会重新下载并校验 Electron、执行完整测试、打包、测试 EXE 及生成 SHA-256。
- 清理目录跟随 electron-builder 的 `directories.output` 配置，并对 Windows 临时文件锁进行有限重试。
- 退出期间托盘点击和第二实例事件不会再次访问已销毁的主窗口。
- 设置作为独立本地标签打开和关闭；首页显示 5 个预设网站，并支持解析、去重和持久化导入的浏览器书签 HTML。
- PNG、JPEG、WebP 文件头识别、每张 2 MB 上限、旧单图设置迁移、去重与 20 张轮播上限。
- 10 张内置广告的文件名连续性、PNG 文件头、720×1020 尺寸与内容体积检查。
- 设置 JSON 首次创建、连续原子覆盖与损坏文件回退。
- 标签 URL 校验、12 标签上限、活动索引约束和 v1.0 单 URL 设置迁移。
- Electron 集成冒烟：直接加载同时声明 `X-Frame-Options: DENY` 和 `frame-ancestors 'none'` 的测试网页。
- Electron 集成冒烟：前进/后退、主地址恢复、远程页面不可访问 Node/Electron API。
- Electron 集成冒烟：创建两个独立 `WebContentsView` 标签、切换标签、标签 URL 隔离和退出前会话落盘。
- Electron 集成冒烟：通过真实鼠标点击触发 Pointer Lock，再由主进程强制释放并验证页面与应用状态均已清除。
- Electron 集成冒烟：真实点击 `target="_blank"` 链接，验证 HTTP GET 页面进入新标签且未创建原生子窗口。
- 本地工具栏与内置广告截图有效性；内置轮播首图/`1 / 10` 计数以及两张临时自定义图的 Data URL/`1 / 2` 计数。
- 便携版 EXE 的独立启动冒烟测试。

## 本次执行结果

- `npm run check`：通过，主进程、preload、核心模块、设置存储与渲染脚本均无语法错误。
- `node --test test/clean-build.test.js test/core.test.js test/settings-store.test.js`：通过，相关 14 项单元测试全部成功。
- `npm test`：15 项中 14 项通过；唯一失败仍是既有资源校验，`build\ads` 含 `ad-11.png`～`ad-13.png`，但应用与测试清单限定为 10 张内置广告。
- v1.8.1 源码 Electron 集成测试：通过；覆盖合并浏览栏收起/恢复、老板键两种动作，以及 electron-builder 遗留 `win-unpacked.tmp` 的打包前清理。
- v1.8.2 首次自动发布在素材测试阶段失败：8 张 `.png` 路径实际包含 JPEG 数据；未生成 Release。
- v1.8.3 素材测试按真实文件头接受应用支持的 PNG/JPEG，并继续校验 10 张内置清单、`440×586` 尺寸和最小体积；自动发布结果见 GitHub Actions。
- v1.8.4 源码 Electron 集成测试：通过；中文“关于本应用”对话框动态显示应用版本，并在应用和 README 中声明项目内容完全由 OpenAI Codex 生成。
- v1.6.1 便携版重新打包并完成独立集成测试：通过；打包前清理正确定位到 `build`，未再出现写入已解包 EXE 的错误。
- Windows x64 便携版 EXE 集成测试：通过。
- v1.4.0 便携版独立启动测试：通过；内置广告为 `1 / 10`，两张临时自定义图为 `1 / 2`。
- 离线 Electron 运行时：`vendor\electron\electron-v42.7.0-win32-x64.zip`，148,117,322 字节，SHA-256 与 Electron 自带 `checksums.json` 一致。
- 拒绝 iframe 测试页：同时返回 `X-Frame-Options: DENY` 和 `Content-Security-Policy: frame-ancestors 'none'`，仍由 `WebContentsView` 直接加载成功。
- 导航：第二页加载、`canGoBack` 与返回第一页均通过。
- 标签管理：创建两个标签、切换回首标签、两个 URL 独立、活动索引与标签列表持久化均通过。
- 标签快捷键：远程网页获得焦点时 `Ctrl+T`、`Ctrl+W`、`Ctrl+Tab` 均通过集成测试；本地工具栏路径由渲染脚本处理。
- 云游戏鼠标：当前活动标签成功获得 Pointer Lock，强制释放返回成功，`document.pointerLockElement` 与应用捕获状态均清空。
- 新窗口路由：`target="_blank"` 测试链接成功进入第二个应用内标签，受限子窗口数量为 0。
- 沙箱：远程页面内 `typeof require`、`typeof process` 和 `typeof window.miniBrowser` 均为 `undefined`。
- UI 视觉检查：10 图总览与老板键广告截图布局正常；内置首图显示 `1 / 10`，两张临时自定义图片显示 `1 / 2`；Windows 实际窗口无障碍树确认标签栏、关闭/新建标签按钮、地址栏和远程网页同时存在。
- `npm audit`：安装时报告 0 个已知漏洞。
- 安装版和便携版均未进行 Authenticode 代码签名，状态为 `NotSigned`，可能触发 SmartScreen 未知发布者提示。

## 人工验收清单

- [ ] 安装版向导安装、开始菜单/桌面快捷方式和卸载。
- [ ] 便携版首次启动与第二次启动聚焦已有窗口。
- [ ] 窗口拖动、八方向原生缩放、显示器切换与窗口位置恢复。
- [ ] 透明度、置顶、最小化、托盘隐藏和托盘菜单。
- [ ] 全局老板键在其他程序获得焦点时触发，冲突时保留旧快捷键。
- [ ] 分别选择“显示广告”和“静音并隐藏”，在普通、托盘隐藏和最小化状态下往返恢复正确。
- [ ] 自定义广告多选、按序轮播、整组替换/删除、格式错误、超过 20 张与单张超出 2 MB 提示。
- [ ] 登录会话、下载“另存为”和安全网页弹窗。
- [ ] 开机启动开启与关闭。

涉及 Windows 安装、卸载、开机自启和其他程序焦点的项目保留为人工验收项，不在构建机上擅自修改系统状态。
