# 常见问题 (FAQ)

## 为什么网页不用 iframe 嵌入?
本项目基于 Electron 的 WebContentsView，每个标签是独立的渲染进程，
因此不受目标网站 X-Frame-Options 或 CSP frame-ancestors 限制。

## 如何切换标签?
使用顶部合并浏览栏点击对应标签即可，最多支持 12 个相互独立的网页标签。

## 鼠标强制释放是做什么的?
云游戏场景下部分页面会锁定指针，本设置可在需要时强制释放鼠标指针。
